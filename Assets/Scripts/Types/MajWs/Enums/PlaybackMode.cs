namespace MajdataViewX.Types.Enums
{
    // 播放模式。线格式契约：成员顺序即数值，必须与 Edit 端一致（Normal, IncludeOp, Record, Preview）。
    public enum PlaybackMode
    {
        Normal,
        IncludeOp,
        Record,
        Preview,
    }
}
